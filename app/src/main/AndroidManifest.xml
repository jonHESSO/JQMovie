<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jqmovie.jqmovie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Movies.MovieEdit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Movies.Movies" />
        <activity android:name=".Actors.Actors" />
        <activity android:name=".Movies.MovieDetails" />
        <activity android:name=".Directors.Directors" />
        <activity android:name=".Directors.DirectorDetails" />
        <activity android:name=".Directors.DirectorEdit" />
        <activity android:name=".Actors.ActorDetails" />
        <activity android:name=".Actors.ActorEdit"></activity>
    </application>
</manifest>