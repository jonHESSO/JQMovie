<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jqmovie.jqmovie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Movies">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Directors" />
        <activity android:name=".Actors" />
        <activity android:name=".MovieDetails" />
        <activity android:name=".ActorDetails" />
        <activity android:name=".DirectorDetails" />
        <activity android:name=".MovieEdit" />
        <activity android:name=".ActorEdit" />
        <activity android:name=".DirectorEdit"></activity>
    </application>

</manifest>